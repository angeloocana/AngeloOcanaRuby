    <div class="row">
        <div class="col-lg-6">
            <div class="well bs-component">
				<%= simple_form_for @label, html: { class: 'form-horizontal' }, wrapper: :horizontal_form do |f| %>
                    <%= f.error_notification %>
                    <%= f.input :name %>
  					<%= f.button :submit %>
				<% end %>
        <br />
        <%= link_to '<-- Back', labels_path %>
         	</div>
        </div>
    </div>